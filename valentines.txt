<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Valentine's Surprise</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #ffb3ba, #ffdfba, #e6e6fa); /* Pastel pink, cream, lavender gradient */
            margin: 0;
            padding: 0;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            position: relative;
        }

        /* Floating hearts animation */
        .heart {
            position: absolute;
            color: #ff69b4;
            font-size: 20px;
            animation: float 6s infinite linear;
            opacity: 0.7;
        }
        @keyframes float {
            0% { transform: translateY(100vh) rotate(0deg); }
            100% { transform: translateY(-100vh) rotate(360deg); }
        }

        /* Initial screen */
        #initial-screen {
            text-align: center;
            z-index: 10;
        }
        #surprise-button {
            background: linear-gradient(45deg, #ff69b4, #ffb3ba);
            border: none;
            border-radius: 50px;
            padding: 20px 40px;
            font-size: 24px;
            color: white;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s, box-shadow 0.3s;
        }
        #surprise-button:hover {
            transform: scale(1.1);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }

        /* Diorama screen */
        #diorama-screen {
            display: none;
            position: relative;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1578662996442-48f60103fc96?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80') no-repeat center center;
            background-size: cover;
            animation: fadeIn 2s ease-in-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Message overlay */
        #message {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            font-size: 28px;
            color: #ff69b4;
            text-align: center;
            animation: slideIn 1s ease-out 1s both;
        }
        @keyframes slideIn {
            from { transform: translate(-50%, -200%); }
            to { transform: translate(-50%, -50%); }
        }
    </style>
</head>
<body>
    <!-- Floating hearts -->
    <div class="heart" style="left: 10%; animation-delay: 0s;">♥</div>
    <div class="heart" style="left: 20%; animation-delay: 1s;">♥</div>
    <div class="heart" style="left: 30%; animation-delay: 2s;">♥</div>
    <div class="heart" style="left: 40%; animation-delay: 3s;">♥</div>
    <div class="heart" style="left: 50%; animation-delay: 4s;">♥</div>
    <div class="heart" style="left: 60%; animation-delay: 5s;">♥</div>
    <div class="heart" style="left: 70%; animation-delay: 0s;">♥</div>
    <div class="heart" style="left: 80%; animation-delay: 1s;">♥</div>
    <div class="heart" style="left: 90%; animation-delay: 2s;">♥</div>

    <!-- Initial screen -->
    <div id="initial-screen">
        <button id="surprise-button">Open Surprise</button>
    </div>

    <!-- Diorama screen -->
    <div id="diorama-screen">
        <div id="message">Happy Valentine's Day! I miss you so much.</div>
    </div>

    <script>
        document.getElementById('surprise-button').addEventListener('click', function() {
            document.getElementById('initial-screen').style.display = 'none';
            document.getElementById('diorama-screen').style.display = 'block';
        });
    </script>
</body>
</html>